{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "routes": [
    { "src": "/api/(.*)", "dest": "/api/index.cjs" },
    { "src": "^/assets/(.*)", "dest": "/assets/$1" },
    { "src": "^/(.*)\\.(.+)$", "dest": "/$1.$2" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
